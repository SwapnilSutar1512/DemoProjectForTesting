# Application
spring.application.name=dammyproject
server.port=2222

# Updated MySQL connection properties for troubleshooting Communications link failure
spring.datasource.url=jdbc:mysql://mysql-235136da-dammyproject.f.aivencloud.com:14955/defaultdb?useSSL=true&requireSSL=true&verifyServerCertificate=false&serverTimezone=UTC&connectTimeout=10000&socketTimeout=30000
spring.datasource.username=avnadmin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Optional: Enable MySQL connection diagnostics
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=5000

# SSL TrustStore configuration (should be set as JVM options, not in properties file)
# -Djavax.net.ssl.trustStore=C:\Users\Acer\Downloads\aiven-truststore.jks
# -Djavax.net.ssl.trustStorePassword=changeit

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
